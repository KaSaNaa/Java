package Arrays;

public class arrays {
    public static void main(String[] args) {

        /*
         * first you have to assign a identifier to the array yoi create.
         * then you can assign how many array slots it can create.
         */

        int[] x = new int[2];

        double array[] = new double[4];

        /*
         * when creating an array, putting sq. brackets after array type is the most
         * commonly used method.
         * but if you like you can this way also.
         */

        array[1] = 100.20;
        x[0] = 50;

        System.out.println(x[0]);
        System.out.println(array[1]);
    }
}
